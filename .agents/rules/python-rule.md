---
trigger: always_on
---

## Environment Rules

- Use **Python** for this project.
- Use **uv** for package and environment management.
- Run commands with **`uv run`**.
- Add packages with **`uv add`**.
- Avoid using `pip` or running `python` directly unless explicitly required.
- Prefer staying within the uv-managed workflow at all times.